查看所有正在运行的进程：
  1、top动态显示
  2、ps静态显示

查看后台工作的序号：
  jobs
 
将正在执行的任务放到后台执行：
  ctr+z 【jobs查出来的序号】
  
查看进程
  ps -ef | grep php
    -e：显示所有进程。
    -f：全格式。
    -h：不显示标题。
    -l：长格式。
    -w：宽输出。
    -a：显示终端上的所有进程，包括其他用户的进程。
    -r：只显示正在运行的进程。
    -x：显示没有控制终端的进程。
    -help：显示帮助信息。
    -version：显示该命令的版本信息。

杀死进程
  kill -9 pid
